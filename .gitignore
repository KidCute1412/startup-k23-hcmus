# --- LaTeX Auxiliary Files ---
*.aux
*.fdb_latexmk
*.fls
*.log
*.out
*.toc
*.synctex.gz
*.bbl
*.blg
*.pdf.bak

# --- VS Code ---
# Giữ lại settings.json để cả nhóm dùng chung cấu hình biên dịch tự động, bỏ qua các cache khác
.vscode/*
.vscode/settings.json
.history/

# --- OS-specific files ---
.DS_Store
Thumbs.db

# --- PDF Output (Tùy chọn) ---
# Mặc định LaTeX sẽ sinh ra file PDF từ code nguồn. 
# Nếu nhóm bạn muốn đẩy file PDF lên GitHub để tiện đọc trực tiếp thì COMMENT dòng dưới.
# Nếu không muốn lưu file PDF trên Git (vì nó tự sinh ra được), hãy UNCOMMENT dòng dưới.
# *.pdf

# --- Backend & Frontend Dependencies & Builds ---
**/node_modules/
**/dist/
**/build/
backend/uploads/
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
backend/docs/tasks

# --- Bruno ---
.bru-state/
*.user.bru

*.http

# CoderMind ignores (managed by `cmind init/update`)
.cmind/*
!.cmind/config.toml
.rpgkit/
.venv_dev/
.cmind_dev_env/
.vscode/mcp.json
.vscode/tasks.json
.mcp.json
.github/agents/
.github/prompts/
*.bcf
*.nav
*.run.xml
*.snm

# --- Codex (cấu hình cá nhân, không commit) ---
.codex/
