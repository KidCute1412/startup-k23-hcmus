meta { name: Update Existing Gear Dates type: http seq: 3 }
put { url: {{baseUrl}}/cart/items/{{gearId}} body: json auth: inherit }
headers { Origin: http://localhost:3000 }
body:json { { "startDate": "2027-08-02", "endDate": "2027-08-06" } }
tests { test("upsert returns 200", function () { expect(res.status).to.equal(200); expect(res.body.success).to.equal(true); }); }
