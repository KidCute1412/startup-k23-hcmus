meta { name: Delete Cart Item type: http seq: 5 }
delete { url: {{baseUrl}}/cart/items/{{cartItemId}} auth: inherit }
headers { Origin: http://localhost:3000 }
tests { test("returns latest cart", function () { expect(res.status).to.equal(200); expect(res.body.success).to.equal(true); }); }
