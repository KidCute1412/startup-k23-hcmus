meta { name: Cart Non Renter type: http seq: 9 }
get { url: {{baseUrl}}/cart auth: inherit }
tests { test("non-renter forbidden", function () { expect(res.status).to.equal(403); expect(res.body.error.code).to.equal("RENTER_ONLY"); }); }
