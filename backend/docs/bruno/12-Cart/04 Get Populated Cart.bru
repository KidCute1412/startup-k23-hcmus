meta { name: Get Populated Cart type: http seq: 4 }
get { url: {{baseUrl}}/cart auth: inherit }
tests { test("contains quoted items", function () { expect(res.status).to.equal(200); expect(res.body.data.items[0]).to.have.property("rentalFee"); }); }
