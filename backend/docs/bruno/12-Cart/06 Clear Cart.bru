meta { name: Clear Cart type: http seq: 6 }
delete { url: {{baseUrl}}/cart auth: inherit }
headers { Origin: http://localhost:3000 }
tests { test("cart is empty", function () { expect(res.status).to.equal(200); expect(res.body.data.items).to.have.length(0); }); }
