meta {
  name: Approve Gear Not Found
  type: http
  seq: 17
}

post {
  url: {{baseUrl}}/admin/gears/{{missingId}}/approve
  body: none
  auth: inherit
}

tests {
  test("returns not found for a missing gear", function() {
    expect(res.status).to.equal(404);
  });
}
