meta {
  name: Approve KYC
  type: http
  seq: 1
}

patch {
  url: {{baseUrl}}/admin/users/:id/kyc
  body: json
  auth: inherit
}

body:json {
  {
    "status": "verified",
    "note": "Thông tin không khớp với ảnh chân dung"
  }
}
