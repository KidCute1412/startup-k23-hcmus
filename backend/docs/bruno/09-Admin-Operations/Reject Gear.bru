meta {
  name: Reject Gear
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/admin/gears/{{gearId}}/reject
  body: none
  auth: inherit
}

tests {
  test("rejects the gear", function() {
    expect(res.status).to.equal(201);
    expect(res.body.data.approval_status).to.equal("rejected");
  });
}
