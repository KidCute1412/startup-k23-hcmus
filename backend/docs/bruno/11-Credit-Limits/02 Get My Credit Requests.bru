meta { name: Get My Credit Requests type: http seq: 2 }
get { url: {{baseUrl}}/credit-limit-requests/me body: none auth: inherit }
tests { test("returns active and history", function () { expect(res.getBody().data).to.have.property("history"); }); }
