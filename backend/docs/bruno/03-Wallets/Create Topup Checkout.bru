meta {
  name: Create Topup Checkout
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/wallets/topups/checkout
  body: json
  auth: inherit
}

body:json {
  {
    "amount": 500000,
    "method": "payos"
  }
}
